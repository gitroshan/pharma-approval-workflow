# Screenshots

Drop PNGs here with these exact names so the README renders them:

- `login.png` — the sign-in screen (shows the distinct demo roles)
- `request-detail.png` — a request in PENDING_APPROVAL with the Approve/Reject controls
- `audit-trail.png` — the Audit view with the green "Chain verified" integrity badge

Capture at a clean browser width (~1200px) for consistent framing.
